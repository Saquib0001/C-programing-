// program to count string length
#include<stdio.h>
#include<string.h>

int main(){
   
   char name[] = "Saquib";
   int length = strlen(name);

   printf("string length are : %d " ,length);

   return 0 ;

}